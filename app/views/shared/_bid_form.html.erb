<%= simple_form_for @bid do |f| %>
  <%= f.error_notification %>
  <%= f.hidden_field :item_id, :value => item.id  %>
  <%= f.input :bid_amount, label: false, input_html: { value:  number_with_precision(item.top_bid_amount + 5, :precision => 2) }, wrapper_html: {class: "form-inline"}  %>
  <%= f.button :submit, 'Bid', :confirm => "Are you sure you want to bid on the #{item.title}?", :class => "btn btn-small btn-primary bid_button inline", :id => 'right_col_input' %>
<% end %>